"use client";

import React from "react";

const Menu = () => {
  return (
    <nav>
      <ul className="flex text-[14px] font-quicksand font-bold flex-col gap-y-6">
        {}
      </ul>
    </nav>
  );
};

export default Menu;
